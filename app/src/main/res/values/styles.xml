<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    
    <style name="button_style" parent="@android:style/Widget.Button">
        <item name="android:background">@drawable/button_selector</item>
        <item name="android:textColor">@color/set2_button_text</item>
        
    </style>
    <style name="edittext_style" parent="@android:style/Widget.EditText">
        <item name="android:background">@color/set2_titles_backgraund</item>
        <item name="android:textColor">@color/set2_titles_text</item>
        <item name="android:textSize">12sp</item>
    </style>
    
    <style name="text_view_main_style" parent="@android:style/Widget.TextView">
<!--        <item name="android:background">@color/set2_titles_backgraund</item>
        <item name="android:textColor">@color/set2_titles_text</item>
          <item name="android:textSize">12sp</item>  -->
    </style>
    

</resources>
